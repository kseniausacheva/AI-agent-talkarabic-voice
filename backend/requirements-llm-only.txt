# Лёгкий subset для проверки только LLM (без torch/whisper).
# Полезно для быстрого smoke-теста OpenRouter перед установкой тяжёлого ML-стека.
openai==1.59.7
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1
